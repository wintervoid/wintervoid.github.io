// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://wintervoid.github.io', // Replace with your GitHub username or custom domain
  base: '/',
});
